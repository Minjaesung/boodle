function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);

  fill(255)

if(mouseX<)  
     quad(mouseX-40, mouseY+25, mouseX+60, mouseY+25, mouseX+40, mouseY-25, mouseX-60, mouseY-25);
}